# GoTube root example.
# Do not put real secrets in this file. Copy package-specific examples instead:
#
# Frontend:
#   cp apps/web/.env.example apps/web/.env.local
#
# Worker local dev:
#   cp worker/.dev.vars.example worker/.dev.vars
#
# The frontend must never contain YouTube API keys or Supabase service-role keys.
